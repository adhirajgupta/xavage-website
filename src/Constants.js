import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#83F4FF', // Primary color
    },
    secondary: {
      main: '#a2e5ec', // Secondary color (example)
    },
  },
});



